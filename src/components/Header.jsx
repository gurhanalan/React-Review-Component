import React from "react";

const Header = () => {
    return (
        <header className="title">
            <h2>
                <span>/</span>Reviews
            </h2>
        </header>
    );
};

export default Header;
